#include<bits/stdc++.h>
using namespace std;
int main ()
{ //int n; cin>>n;
    vector<int> a(10);
    vector<int> b;
    int p=a.size();
    for(int i=0; i<=p; i++)
    {
        cin>>b[i];
        if(a[10]==b[i]) cout<<b[i]<<" "<<endl;
    }

 return 0;
}
